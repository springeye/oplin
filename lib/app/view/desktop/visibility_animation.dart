import 'package:flutter/material.dart';

class VisibilityAnimation extends StatelessWidget {
  const VisibilityAnimation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
